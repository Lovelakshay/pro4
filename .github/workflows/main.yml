name: Hello Action

on:
  push:
    branches:
      - main

jobs:
  hello_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print Hello
        run: echo "Hello Lakshay, GitHub Action RUN ho gaya 🎉"
